# 待办与未验证项

诚实记账。**已实测的给证据，没证据的明确标出来。**
不要因为一份 TODO 好看，就把"应该能跑"当成"跑过了"。

最后更新：2026-09-13

---

## ✅ 已在服务器实测（有日志/文件为证）

| 项 | 证据 |
|---|---|
| 取回群视频原片 | NapCat 插件日志：下载 51MB 原片到 `video-fetch/videos/` |
| 自适应抽帧 + 视觉理解 | 日志：`帧数=19 耗时=6~26s`，描述含游戏名/地图/界面文字 |
| 视觉时间轴（带时间戳分段） | 日志：`00:01 一只手捏着两块白色半透明方形果块…` |
| 时间轴落盘 | `MaiBot/data/plugins/ji-or-ji.video-understanding/timeline/*.json` 实际存在 |
| 真实群号 / 消息 id | 日志：`group=1076711748 msg=1100543097` |
| 重复视频不重复干活 | 同一条视频第二次处理命中缓存 / 按文件名复用 |
| 组件权限 | `component.enable/disable` 加入 manifest 后，权限警告消失 |
| 会话隔离 | 多群实测（1076711748 / 837715340）各记各的，无串台 |
| **③ 级按需重读** | 模型自主调用 `read_video`，回传 3~6 秒画面描述，bot 据此答「第四秒…白T恤那哥们坐椅子上操作」 |

---

## ⚠️ 未验证（无证据，不要当成能用）

| 项 | 现状 | 怎么验 |
|---|---|---|
| **声画双轨** | 摘要合并逻辑只在**本地合成的音频**上测过；服务器上跑的视频都没有语音，双轨从未真实合流 | 群里发一条**有人说话**的十几秒视频，看摘要是否出现 `语音：…` |
| **音频链路（api 模式）** | 服务器配的是 `audio.mode = api`，**从没实际调用成功过**（本地只测过 `local`） | 同上，看日志 `used=api` |
| **分块转录参数** | `block_seconds` / `gap_seconds` / `max_audio_seconds` 只在合成素材（重复语音）上调过，真实断续对话未测 | 同上，看实际调用次数 |
| **合并转发** | 只确认了 `ctx.actions.call` 存在，**代码尚未实现** | 需先实现，再发合并转发验证 |
| **跨会话引用反查** | `message_id` 已正确落盘，但**引用提问的链路没走通过** | 引用一条旧视频消息提问 |

---

## 已知边界（不打算修）

- 转发消息里只带发送方手机路径（`/storage/emulated/...`），服务器无实体，直接跳过
- 时间轴粒度跟抽帧密度，稀疏区的问题答不细（要精确得开 `keep_video`）
- ASR 分块后音频时间戳较粗（约 2 分钟一段），定位精确时刻靠视觉侧
- 麦麦插件的 `@Tool` 是导入期注册，工具无法按配置动态增删；改用 `get_components` 置禁用态控制可见性

---

## 后续

- [ ] **合并转发**：用 `ctx.actions.call('get_forward_msg', ...)` 取回内层，递归找视频段（递归已有深度上限 8 + 去重护栏）
- [ ] 一次真实语音视频的端到端验证（同时覆盖上面四项音频相关）
- [ ] 确认 `component.enable/disable` 在**配置热改**时也能同步（目前只验证了启动时无警告）
- [ ] 视需要接入麦麦插件市场的发布流程（参见 `STORE.md`）

---

## 验证清单（给未来的自己）

发一条**有语音**的视频后，这几条日志要一起出现：

```
检测到 1 个视频 session=... group=1076711748 msg=...
时间轴已落盘 key=... group=1076711748 msg=... segs=N
视频理解完成 name=... text=00:02 ... / 语音：...
```

并且检查 `timeline_index.json` 里那条记录的 `summary` 是否**同时含「画面」和「语音」**。
若只有「画面」，说明音频轨道没合进去，回去查 `audio_transcript` 的 `segments` 是否为空。
