# 麦麦假装看视频

让麦麦看得懂群里发的视频。

## 它解决什么

原本麦麦对群里的视频是「瞎的」——**QQ 收到视频不会下载原片**，只存一张封面缩略图；
adapter 又把视频段压成纯文本，把下载地址丢掉。所以机器人能收到"有人发了视频"，
但拿不到视频本身，自然也说不出内容。

本插件把这条路打通：**取回视频 → 看懂内容 → 作为上下文交给麦麦**，全程不打扰聊天。

## 核心：帧数是算出来的，不是定时抽的

| | 常见做法 | 本插件 |
|---|---|---|
| 抽帧 | 固定间隔（如每 2 秒一张） | 按画面信息密度自适应分配 |
| 依据 | 时钟 | 编码码率曲线 |
| 结果 | 一小时的重复内容照样抽满 | 变化剧烈处密、静止处疏 |

实现方式：用 `ffprobe` 读每个 packet 的码率，当作「画面信息密度」的代理，
把密度曲线转成累积分布后按预算撒点——密度高处自然被更多点命中。

配套两个机制：

- **新颖率检测**：抽一批探针帧，比对前后半段的覆盖度。整片重复的内容（比如 5 秒循环
  播放的视频）会被识别出来，帧数压到个位数。
- **签名去重**：按帧指纹生成视频签名并缓存。同一条视频再出现（哪怕是不同的压缩版本、
  不同分辨率），直接复用已有描述，**跳过全部模型调用**。

## 音频：三档可选

| 档位 | 说明 |
|---|---|
| `off`（默认） | 不读音频，零内存占用 |
| `local` | 本地 sherpa-onnx SenseVoice，无网络、不外传 |
| `api` | 云端 ASR，带超时降级 |

转录**带时间戳**：按静音切出语音区间、合并成块后逐块转录，每段知道自己对应哪一秒。
音频与画面是**两条平行轨道**，最后在摘要层汇合（见下节）。

本地推理约占 322MB。为此配了三道护栏，内存吃紧的机器也能放心开：

- **内存闸门**：加载前查可用内存，不达标就不加载，直接降级
- **失败回退**：api ↔ local 自动切换
- **用完即卸**：转录完立刻释放模型

调用次数可调：停顿合并阈值、单块最大时长、总时长上限都是配置项，按自己的计费方式选。

## 看得懂时间点

「视频 10:03 那里是什么意思」这类问题，靠两层回答：

| 层 | 存哪 | 成本 |
|---|---|---|
| 压缩摘要（常驻） | 注入上下文 | 每轮对话都付，固定上限 |
| 完整时间轴（按需） | 落盘 | 零模型成本，问才查 |

摘要里同时带画面与语音：

```
00:04 画面：游戏内商店界面… / 语音：「首领第一次狂暴的时候…」
```

**预算共用，不是各占一份**：只有一边有内容就独占整行，两边都有各分一半。
所以不管视频多长、有没有语音，注入体积都是固定的（最多 6 行 282 字符）。

开 `keep_video` 后还能解锁「重读某段」：在指定区间固定密度补抽 8 帧，
实测 0.5~0.6 秒（纯本地）。

## 低消耗

| 维度 | 表现 |
|---|---|
| 成本 | 单条视频约 1 分钱，月成本个位数 |
| 算力 | 重复画面不重复抽，5 分钟循环视频只抽 3 帧 |
| 重复开销 | 同一视频第二次 0.4~0.6 秒复用，不调模型 |
| 磁盘 | 理解完自动清理视频与中间文件 |
| 延迟 | 后台处理不阻塞聊天，多帧一次进模型 |
| 常驻上下文 | 摘要固定上限（6 行 282 字符），视频多长都不涨 |

## 安装要点

**⚠️ 有硬前置：必须同时装一个配套的 NapCat 插件**（`video-fetch`）。
因为 QQ 不下载群视频原片，必须从 NapCat 的原始事件里拿真实 CDN 地址并下载。

1. 本插件放进麦麦的 `plugins/`
2. `video-fetch` 放进 NapCat 的 `plugins/`，并在 `napcat/config/plugins.json` 写
   `{"video-fetch": true}`（NapCat 除内置插件外默认禁用）
3. 重启 NapCat
4. **务必配置 `ffmpeg_path` / `ffprobe_path` 为绝对路径**——麦麦进程可能继承很旧的
   PATH，`shutil.which("ffmpeg")` 会返回 `None`，抽帧直接报错
5. 配置 `napcat.fetch_dir` 指向 `video-fetch` 的下载目录

## 工程细节（给想读代码的人）

- **描述注入放在上下文末尾**：模型开了 prompt 前缀缓存，前缀一变缓存全废，按全价重算。
- 注入用的钩子是 `maisaka.replyer.before_model_request`，拿的是麦麦自己的 ContextItem
  列表（不是 OpenAI messages），返回值必须带 `item_schema_version`，否则整包被丢弃。
- 等待取回文件时按「大小连续两次采样一致」判定写完，避免把半截文件喂给 ffprobe。

## 已知边界

- **合并转发**暂不支持：OneBot 只给一个 `id`，内容需额外请求取回（`ctx.actions.call` 已确认可用）
- 转发消息里若只带发送方手机路径（`/storage/emulated/...`），服务器无实体，会快速跳过
- 时间轴粒度跟抽帧密度，稀疏区的问题答不细；要精确请开 `keep_video` 走重读
- 跨会话提问需要**引用原消息**（reply 段给消息 id，据此反查）

## 许可

PolyForm Noncommercial 1.0.0（**非商用**，不是开源协议）：可自由使用、修改、分发
（含闭源），但**不得用于商业用途**。
