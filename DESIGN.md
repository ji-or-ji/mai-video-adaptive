# mai-video-adaptive 设计说明

**本文只回答一个问题：为什么这么设计。**

| 文件 | 读者 | 内容 |
|---|---|---|
| `README.md` | 使用者 | 能做什么、怎么装、怎么配、做不到什么 |
| `DEVELOPING.md` | 改代码的人 | 内部机制、契约、踩过的坑、测试 |
| `DESIGN.md`（本文） | 做判断的人 | 设计取舍与实测依据 |
| `TODO.md` | — | 未验证项与后续计划 |

让视频按内容自动决定**抽多少帧、抽在哪**，配合语音转录交给视觉模型理解。
成本压在分币级、耗时压在秒级，重复视频直接复用结果。

交付形态：麦麦插件（仓库根目录即插件本体）+ 配套 NapCat 插件（`napcat-plugin/video-fetch/`）。
硬前置是必须同时装那个 NapCat 插件——原因见 `DEVELOPING.md`，安装见 `README.md`。

---

## 设计原则：便宜的常驻，贵的按需

整套方案的分账口径。

理解一条视频要花钱，但花在哪、付几次，差别极大：

| 类型 | 什么时候付 | 内容 |
|---|---|---|
| **一次性** | 只在理解时付一次 | 抽帧、音频转录、视觉模型调用 |
| **常驻** | 每轮对话都在付 | 注入上下文的文本 |

所以：**重的放一次性，轻的放常驻**。做法是把理解结果切成两份：

| 版本 | 存哪 | 体积 | 用途 |
|---|---|---|---|
| 完整时间轴 | 落盘 | 随视频长度增长 | 需要细节时按需查 |
| 压缩摘要 | 注入上下文 | **固定上限**（6 行 ≈ 282 字符） | 常驻，日常问答用 |

**关键在固定上限**：10 秒的视频和 10 分钟的视频，注入成本一样。

这条原则也解释了后面所有取舍：为什么时间轴要落盘、为什么摘要要瘦身、
为什么重读要按需触发而不预先做。

## 声画双轨

视频有两条信息轨道，都带时间戳：

| 轨道 | 来源 |
|---|---|
| 画面 | 自适应抽帧 |
| 语音 | 分块转录（按静音切分） |

两条轨道**分开存**（谁看到的、谁说的，各归各处），**只在摘要层合并**：

```
00:04 画面：游戏内商店界面… / 语音：「首领第一次狂暴的时候…」
```

合并时**共用同一份预算**（6 行 / 282 字符）：

- 只有一边有内容 → 独占整行
- 两边都有 → 各分一半

**这个设计解决两个具体问题**：黑屏但有说话的视频不会丢内容（否则摘要全是
「画面全黑，无可见内容」）；也不会因为多了音频就让常驻体积翻倍。

## 视频时间点问答

### 四级降级链

```
① 上下文里的压缩摘要    理解时注入，命中率最高，成本固定
② 本地完整时间轴        零模型成本，毫秒级
③ 重读视频片段          需要 keep_video = true
④ 如实说不知道          兜底，不编
```

①② 始终可用，与视频是否保留无关；③ 由配置解锁；④ 是底线。

### 为什么 ③ 用固定密度补帧

抽帧是**按密度撒点**，不保证第 10 秒恰好有帧。所以重读时在目标区间
**固定密度补抽 8 帧**——区间小，要的是精度不是效率，用不着自适应那套。

（反过来，理解阶段必须用自适应，否则长视频的帧预算会被重复画面吃光。两个阶段
目标不同，所以用两套抽帧策略。）

### 引用提问靠 message_id，不靠 @

用户隔几小时再问时，正常人的做法是**引用那条消息**。
判断依据是 reply 段里的 `message_id`，**不是 @**——引用消息时 QQ 会自动 @ 被引用者，
那个 @ 是噪音，拿它当依据会误判。

### 为什么时间轴粒度不做兜底

时间轴跟抽帧密度走，疏密不均：变化剧烈处细、静止处粗。落在稀疏区的问题，
精度天然有限（可能只有一条跨 2 分钟的粗描述）。

**决定接受这个代价，不做「最大段长」兜底。** 本插件的卖点是省 token，
为泛需求补精度会牺牲整体。精度不够时走 ③ 重读，或如实说不知道。

### 多视频歧义交给模型

同一群里短时间内发两条，用户说「那个视频」——不做消歧逻辑，交给模型按上下文判断。
判断不了就问，不猜。

## 已知边界

- 时间轴粒度跟抽帧密度，稀疏区的问题答不细。要精确请开 `keep_video` 走重读
- 跨会话提问需要**引用原消息**。只口头说「刚才那个视频」且上下文已过期时，答不了
- 合并转发里的视频取不到（OneBot 只给一个 id，需额外请求展开）
- 本地音频模式吃 CPU：内存小可以开本地，**CPU 弱反而该用云端**（实测数据见 `DEVELOPING.md`）

## 实测数据

| 场景 | 结果 |
|---|---|
| 74s 游戏视频 | 19 帧，描述含游戏名 / 地图 / 密码 / 收益 |
| 5 分钟循环视频 | 3 帧（新颖率 0.00），描述准确 |
| 57s 语音视频 | 12 帧 + 本地转录，全程 9s |
| 重复视频第二次 | 0.4~0.6s 复用，跳过 API |
| 时间轴瘦身后 | 常驻注入 281 字符，时间点问答仍准 |
| 40s 三段语音 | 停顿合并阈值从 1.5 调到 6.0，ASR 调用 3 次 → 1 次 |
| ③ 级重读（服务器） | 模型自主回看 3~6 秒，答「第四秒还在电脑前，白T恤那哥们坐椅子上操作呢」 |
| 取回暂存区 | 加 TTL 前 7 天堆 46 文件 / 667MB；加后启动即清 43 个 |

## 状态

- [x] 自适应抽帧（码率密度 + CDF 撒点）
- [x] 新颖率与动态上下限
- [x] 音频链路（抽轨 / 静音分析 / 分块转录，含时间戳）
- [x] 本地 ASR（sherpa-onnx SenseVoice）
- [x] 三档音频模式（含内存闸门与失败回退）
- [x] 端到端理解与防幻觉约束
- [x] 签名缓存去重
- [x] 带时间戳的分段描述（时间轴）+ 落盘 + 按需检索
- [x] 注入改用压缩摘要（固定上限），声画双轨合并
- [x] ③ 级按需重读（保留原片 + `read_video` 工具，已实测）
- [x] 接入麦麦插件（仓库根目录即插件本体）
- [x] 配套 NapCat 插件取回群视频（`napcat-plugin/video-fetch/`）
- [x] 会话隔离（严格按 session，不跨会话兜底）
- [x] 取回暂存区 TTL 清理
- [ ] 合并转发（`ctx.actions.call` 已确认可用，未实现）
